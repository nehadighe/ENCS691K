"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildResources = void 0;
const ora_1 = __importDefault(require("ora"));
const get_provider_plugins_1 = require("./get-provider-plugins");
const spinner = ora_1.default('Building resources. This may take a few minutes...');
function buildResources(context, category, resourceName) {
    const confirmationPromise = context.input.options && context.input.options.yes
        ? Promise.resolve(true)
        : context.amplify.confirmPrompt('Are you sure you want to continue building the resources?');
    return confirmationPromise
        .then(answer => {
        if (answer) {
            const providerPlugins = get_provider_plugins_1.getProviderPlugins(context);
            const providerPromises = [];
            Object.keys(providerPlugins).forEach(provider => {
                const pluginModule = require(providerPlugins[provider]);
                providerPromises.push(pluginModule.buildResources(context, category, resourceName));
            });
            spinner.start();
            return Promise.all(providerPromises);
        }
    })
        .then(() => spinner.succeed('All resources are built.'))
        .catch(err => {
        spinner.fail('An error occurred when building the resources.');
        throw err;
    });
}
exports.buildResources = buildResources;
//# sourceMappingURL=build-resources.js.map